# Wire Time Mixer Gates GB10

- Date: 2026-05-16.
- Branch/worktree: `kernel-tuning-wire-time-mixer-gates-gb10-20260516` in the existing dirty workspace.
- Dirty-tree constraint: this checkout carries broad unrelated uncommitted workspace changes and prior kernel-tuning notes. This attempt is scoped to wiring existing time-mixer gate kernels into the model path plus this note.
- Prior-note search command:
  - `rg -n "learning_rate_gate|value_residual_gate|gate forward|value residual|learning rate.*custom|wired|wire" .agents/notes/kernel-tuning .agents/skills/kernel-tuning/SKILL.md -S`
  - `rg -n "forward_without|bias\\(|\\.bias|LoRA" crates/rwkv-nn/src -S`
- Matched prior evidence:
  - `2026-05-16-forward-elementwise-autotune-key.md` and `2026-05-16-forward-elementwise-full-hardware-key.md` expanded the forward elementwise keys for `learning_rate_gate` and `value_residual_gate`; they were key-design work and did not wire these kernels into `WeightPrepare`.
  - `2026-05-16-remote-time-mixer-evidence.md` found missing remote tuner logs for `learning_rate_gate` and `value_residual_gate`, consistent with those kernels not being used in the forward path.
  - `2026-05-16-remote-matmul-surface-attribution.md` identified the current `WeightPrepare::forward` Burn expression path as the source-level implementation boundary: `sigmoid(lora.forward(...))` and Burn `sigmoid + lerp` are still live.
  - Do not repeat residual-add A/B, channel-mixer forced Cube/Burn-reference/fusion, LocalTuner bypass, WKV7 forced `row_tile=16`, or lm-head online softmax.
- Machine/GPU: primary validation on remote `caizus@10.100.1.253`, `NVIDIA GB10`, compute capability `12.1`; local compile only.
- Shape/dtype: CUDA BF16 `rwkv_lm`, `B=16,T=512,D=768`, rows `8192`, regenerated remote baseline under `~/Projects/Packages/rwkv-rs-test/test_gen/rwkv_lm/bf16/case_000000`.
- Hypothesis: wiring existing fused `learning_rate_gate` and `value_residual_gate` kernels into `WeightPrepare::forward` will reduce generic Burn elementwise/cast launches in the TimeMixer path while preserving Cubek matmul for LoRA. This should improve or preserve remote total speedup and may reduce the large generic elementwise surface seen in nsys.
- Candidate parameters: no new kernel parameters; reuse existing gate autotune line-size candidates and hardware/shape keys. Add only the model-path wiring and minimal LoRA helper methods needed to separate NoOP+bias LoRA into matmul output plus bias vector.
- Expected keep/revert boundary: keep only if local `cargo check -p rwkv-nn --features cuda` passes, remote `cargo check -p rwkv-test --features cuda` passes, remote activation stays `54/54 PASS`, and standard remote compare improves or at least preserves current `1.48x` without adding failures. If activation drifts or remote timing regresses, revert the live code and keep this branch/note as negative evidence.
- Next command: edit `LoRA` helper methods and `WeightPrepare::forward`, then run nightly rustfmt and local CUDA compile gate.
- Code change: added `LoRA::forward_without_bias` and `LoRA::bias_1d`, then wired `WeightPrepare::forward` to compute NoOP LoRA matmul output separately from the bias vector and call existing `learning_rate_gate` / `value_residual_gate` kernels. `TimeMixer::forward` bounds now include `LearningRateGateBackend` and `ValueResidualGateBackend`.
- Next command: run `rustup run nightly rustfmt` on the touched files, then local `cargo check -p rwkv-nn --features cuda`.
- Local compile gate: `rustup run nightly rustfmt crates/rwkv-nn/src/layers/lora.rs crates/rwkv-nn/src/modules/time_mixer/mod.rs crates/rwkv-nn/src/modules/time_mixer/weight_prepare.rs` passed, and `cargo check -p rwkv-nn --features cuda` passed.
- Next command: sync only the three touched source files and this note to `caizus@10.100.1.253:~/Projects/Packages/rwkv-rs-stable`, then run remote `cargo check -p rwkv-test --features cuda`.
- Remote sync and compile gate: scoped `rsync -avR` of `lora.rs`, `time_mixer/mod.rs`, `weight_prepare.rs`, and this note completed with the explicit GB10 SSH key. Remote `cargo check -p rwkv-test --features cuda` passed on `~/Projects/Packages/rwkv-rs-stable`.
- Next command: run standard remote compare with regenerated remote baseline: `cargo run --release -p rwkv-test --features cuda -- compare-rwkv-nn --color never --baseline ~/Projects/Packages/rwkv-rs-test/test_gen/rwkv_lm/bf16/case_000000 --repeat 3 --warmup 1`.
- Remote compare result: activation stayed valid (`activation_summary compared=54 passed=54 failed=0`). Timing fully passed (`timing_summary compared=76 passed=76 failed=0 actual_total_ms=91.595 baseline_total_ms=175.887 speedup=1.92x`). This is a real improvement over the prior reverted state (`actual_total_ms=118.963`, speedup `1.48x`, one `cell_0000/channel_mixer` timing failure).
- Module timing signal: `cells/*/time_mixer` dropped to `60.071ms` from the prior remote evidence around `90ms`; every cell time-mixer row passed, and the previous `cell_0000/channel_mixer` failure also disappeared (`1.597ms` vs baseline `1.668ms`, `1.04x`).
- Keep/revert decision: keep the live gate wiring unless a follow-up profiler proves this was a stale binary or hidden regression.
- Next command: run a short remote `nsys` profile on the changed binary to verify the gate kernels appear in the CUDA timeline and to compare the generic elementwise surface against the previous remote nsys evidence. Use profiler output only for attribution, not acceptance timing.
- Remote nsys result: `nsys profile --trace=cuda,nvtx --stats=true --force-overwrite=true -o target/rwkv-test/nsys-time-mixer-gates-gb10 ... --repeat 1 --warmup 1` generated `.nsys-rep` and `.sqlite`. The command exited nonzero because compare timing metadata intentionally mismatched `repeat=1` actual against the `repeat=3` baseline; activation still passed (`54/54 PASS`). Use only CUDA kernel stats from this profile.
- Profiler signal after wiring:
  - New custom gate kernels are present: `value_residual_gate_forward_kernel_f__n_1` totals `6.534ms / 33`, and `learning_rate_gate_forward_kernel_f__n_8` totals `2.201ms / 36`.
  - The previously large generic f32 elementwise surface is effectively removed: `kernel_scalar_binop_c_f32_n_4` is now `0.762ms / 108` versus old evidence `37.640ms / 207`, and `unary_float_f_f32_n_4` is now `0.695ms / 108` versus old evidence `24.875ms / 138`.
  - BF16 TMA matmul remains the dominant family (`120.828ms / 327`), followed by remaining custom kernels: WKV7 output `22.523ms / 36`, mix6 `14.427ms / 36`, channel-mixer relu-square `14.065ms / 36`, and lm-head CE row `13.241ms / 3`.
- Final decision for this branch: keep the change. It is correctness-clean on the remote trace, improves standard remote total speedup from `1.48x` to `1.92x`, removes all timing failures, and profiler evidence matches the intended implementation mechanism.
- Continuation command: run the same current gate-wiring code on the local machine for diagnostic/local acceptance coverage: `cargo run --release -p rwkv-test --features cuda -- compare-rwkv-nn --color never --repeat 3 --warmup 1`. This does not change the implementation boundary; record activation and timing before choosing the next implementation branch.
- Local compare result: activation stayed valid (`activation_summary compared=54 passed=54 failed=0`), but timing remained below local acceptance (`timing_summary compared=76 passed=10 failed=66 actual_total_ms=46.834 baseline_total_ms=35.957 speedup=0.77x`). The gate wiring fixed the TimeMixer aggregate locally (`cells/*/time_mixer` `27.369ms` vs `27.836ms`, `1.02x`), so the remaining local blockers are outside this branch: `cells/*/channel_mixer` `0.51x`, residual timing rows around `0.16x`, pre-layer-norm rows around `0.24x-0.26x`, `lm_head` `0.27x`, and `loss/l2wrap_cross_entropy` `0.66x`.
- Local decision: keep the gate wiring because it preserves correctness and improves the targeted TimeMixer path. Open a separate local post-gates audit branch before any profiler run or implementation change for the remaining blockers.
