# Wire Custom Residual Add

- Date: 2026-05-16.
- Branch/worktree: `kernel-tuning-wire-custom-residual-add-20260516` in the existing dirty workspace.
- Dirty-tree constraint: this checkout carries accumulated kernel, skill, fixture, and crate-structure changes from earlier tuning attempts. This branch edits only residual-add call sites and does not change the residual kernel implementation or repeat the Burn-add A/B.
- Prior-note search command: `rg -n "residual_add|Burn add|plain Burn|trace writer|custom residual|sync ablation|0\\.69x|0\\.77x" /root/.codex/memories/extensions/ad_hoc/notes /root/.codex/memories/MEMORY.md .agents/notes/kernel-tuning -S`.
- Matched prior evidence:
  - `/root/.codex/memories/extensions/ad_hoc/notes/2026-05-15T21-10-06-rwkv-nn-timing-optimization-iterations.md` says the custom residual-add kernel was better than switching the trace writer back to plain Burn tensor add, and the stable direction was to restore/use the custom residual path.
  - `/root/.codex/memories/extensions/ad_hoc/notes/2026-05-15T21-26-58-rwkv-nn-sync-ablation.md` says alias/in-place output reuse for `residual_add` and `channel_mixer_relu_square` improved the previous stable run from about `0.69x` to about `0.77x`.
  - `.agents/notes/kernel-tuning/2026-05-16-residual-add-hardware-key.md` added hardware/shape keyed `LocalTuner` to the custom residual path and kept it as a design correction after activation passed.
  - Current source inspection showed `crates/rwkv-nn/src/cells/causal.rs` and `crates/rwkv-test/src/rwkv_nn_trace/writer.rs` still use plain `lhs + rhs`, while `rg -n "residual_add\\(" crates/rwkv-nn/src crates/rwkv-test/src -S` only finds the kernel wrapper definition and trace helper calls, not live custom-kernel call sites.
- Machine/GPU: local CUDA machine, BF16 `rwkv_lm` trace fixture.
- Kernel/stage: per-cell residual writes `embedded_context_after_time_mixer` and `embedded_context_after_channel_mixer`, shape `[16,512,768]`, dtype BF16.
- Hypothesis: the active model and trace writer are not using the already-kept custom residual-add path. Wiring `residual_add(...)` into both call sites should reduce post-stage residual timings without changing math or repeating the old Burn-vs-custom experiment.
- Candidate parameters: existing residual line-size autotune candidates `[1,2,4,8,16,32,64]` and hardware/alias key from the previous branch.
- Expected keep/revert boundary: keep if `cargo check -p rwkv-nn --features cuda` and `cargo check -p rwkv-test --features cuda` pass, activation remains `54/54 PASS`, and the standard compare improves the residual timing rows without regressing total speedup. Revert call-site wiring if activation drifts or total timing regresses.
- Code change: added `ResidualAddBackend` to the `TrainBackend` aggregate trait, changed `CausalCell::forward` residual updates to call `residual_add(...)`, and changed `TraceWriter::trace_residual_add` to call the same custom residual path.
- Planned commands: `rustup run nightly rustfmt crates/rwkv-nn/src/kernels/train/mod.rs crates/rwkv-nn/src/cells/causal.rs crates/rwkv-test/src/rwkv_nn_trace/writer.rs`; `cargo check -p rwkv-nn --features cuda`; `cargo check -p rwkv-test --features cuda`; standard compare.
- Format/check result: rustfmt passed; `cargo check -p rwkv-nn --features cuda` passed; `cargo check -p rwkv-test --features cuda` passed.
- Compare command: `cargo run --release -p rwkv-test --features cuda -- compare-rwkv-nn --color never --repeat 3 --warmup 1`.
- Compare result: activation stayed valid (`activation_summary compared=54 passed=54 failed=0`), but timing regressed: `timing_summary compared=76 passed=7 failed=69 ... actual_total_ms=47.558 baseline_total_ms=35.957 speedup=0.76x`. Residual rows did not improve: `cells/*/embedded_context_after_time_mixer` was `1.388ms` vs `0.246ms` (`0.18x`) and `cells/*/embedded_context_after_channel_mixer` was `1.764ms` vs `0.249ms` (`0.14x`).
- Decision before revert: reject this call-site wiring in the current key-design tree. This differs from the earlier ad-hoc note because the current custom residual path now goes through the hardware-keyed `LocalTuner`; the existing implementation does not beat plain Burn add at the measured trace boundary. Revert only the call-site and aggregate-trait wiring, keep the custom kernel and hardware-key note as implementation evidence.
- Keep/revert state: reverted the call-site and aggregate-trait wiring. `rg -n "residual_add\\(|residual_add::residual_add|ResidualAddBackend" crates/rwkv-nn/src/cells/causal.rs crates/rwkv-nn/src/kernels/train/mod.rs crates/rwkv-test/src/rwkv_nn_trace/writer.rs` now finds only the trace helper name, not live custom residual calls or aggregate trait wiring. Revert validation: rustfmt passed and `cargo check -p rwkv-test --features cuda` passed.
