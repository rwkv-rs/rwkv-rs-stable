# LM Head Forward Online Softmax GB10

- Date: 2026-05-16.
- Branch/worktree: `kernel-tuning-lm-head-online-softmax-gb10-20260516` in the existing dirty workspace.
- Dirty-tree constraint: this checkout carries broad unrelated uncommitted workspace changes from previous train-kernel and crate-structure work. This attempt is scoped to `crates/rwkv-nn/src/kernels/train/lm_head_l2wrap_ce/kernel.rs`, the remote synced copy under `caizus@10.100.1.253:~/Projects/Packages/rwkv-rs-stable`, and this note.
- Prior-note search command:
  - `rg -n "lm_head_l2wrap_ce|forward row|online softmax|logsumexp|log-sum-exp|target-logit|atomic|GB10|remote|10\\.100\\.1\\.253|loss row" .agents/notes/kernel-tuning /root/.codex/memories/MEMORY.md -S`
- Matched prior evidence:
  - `2026-05-16-lm-head-forward-online-softmax.md` tried the same one-pass online log-sum-exp algorithm locally. It passed activation but regressed local diagnostic compare to `speedup=0.77x`; after focused local ncu it was reverted. This branch is allowed only because the hardware/baseline boundary changes to remote GB10.
  - `2026-05-16-remote-time-mixer-evidence.md` shows the remote nsys timeline: the lm_head projection matmul takes about `16ms`, followed by `lm_head_l2wrap_ce_forward_row_kernel` at about `4.2ms` and finalize at about `0.015ms`.
  - `2026-05-16-remote-channel-mixer-edge-ncu.md` records that remote ncu counter collection is blocked by `ERR_NVGPUCTRPERM`; use standard remote compare and nsys timeline evidence unless permissions change.
- Machine/GPU: remote `caizus@10.100.1.253`, `NVIDIA GB10`, compute capability `12.1`; local timing is intentionally avoided because the user is using the local GPU.
- Kernel/stage: CUDA BF16 `lm_head_l2wrap_ce` forward row kernel, shape `B=16,T=512,rows=8192,vocab=65536`, remote regenerated baseline `~/Projects/Packages/rwkv-rs-test/test_gen/rwkv_lm/bf16/case_000000`.
- Changed boundary: repeat the online-softmax row algorithm only on the remote GB10 regenerated-baseline environment. Do not treat the prior local negative result as universal.
- Hypothesis: on GB10, reducing the row kernel from two full-vocab scans to one online log-sum-exp scan may reduce the remote `lm_head_l2wrap_ce_forward_row_kernel` time enough to improve `loss/l2wrap_cross_entropy`, even though the same implementation was not kept locally.
- Candidate parameters: keep existing block-size candidates `[256,512,1024]`; only change the per-row algorithm.
- Expected keep/revert boundary: keep only if remote `cargo check -p rwkv-test --features cuda` passes, remote activation stays `54/54 PASS`, and standard remote compare with `--repeat 3 --warmup 1 --baseline ~/Projects/Packages/rwkv-rs-test/test_gen/rwkv_lm/bf16/case_000000` improves or at least preserves the current remote total while not adding timing failures. If remote compare regresses, revert the live code and keep this branch/note as remote negative evidence.
- Next command: edit `crates/rwkv-nn/src/kernels/train/lm_head_l2wrap_ce/kernel.rs` to the one-pass online log-sum-exp row algorithm, then run nightly rustfmt and local `cargo check -p rwkv-nn --features cuda` as a compile gate before remote sync.
- Code change: changed `lm_head_l2wrap_ce_forward_row_kernel` from separate max and exp/sum scans to a per-thread online log-sum-exp accumulator, then rescales each thread-local sum by `exp(local_max - row_max)` after block max reduction. Target logit is loaded directly by scalar index.
- Local compile gate: `rustup run nightly rustfmt crates/rwkv-nn/src/kernels/train/lm_head_l2wrap_ce/kernel.rs` passed, and `cargo check -p rwkv-nn --features cuda` passed.
- Next command: sync only the changed kernel file to `caizus@10.100.1.253:~/Projects/Packages/rwkv-rs-stable/crates/rwkv-nn/src/kernels/train/lm_head_l2wrap_ce/kernel.rs`, then run remote `cargo check -p rwkv-test --features cuda`.
- Remote sync result: scoped rsync of only `crates/rwkv-nn/src/kernels/train/lm_head_l2wrap_ce/kernel.rs` completed using the explicit GB10 SSH key.
- Next command: `ssh -i ~/.ssh/id_ed25519_dgx_spark_windows -o BatchMode=yes -o ConnectTimeout=10 caizus@10.100.1.253 'cd ~/Projects/Packages/rwkv-rs-stable && cargo check -p rwkv-test --features cuda'`.
- Remote compile gate: `cargo check -p rwkv-test --features cuda` passed on `spark-35ac`.
- Next command: run standard remote compare with regenerated remote baseline: `ssh -i ~/.ssh/id_ed25519_dgx_spark_windows -o BatchMode=yes -o ConnectTimeout=10 caizus@10.100.1.253 'cd ~/Projects/Packages/rwkv-rs-stable && cargo run --release -p rwkv-test --features cuda -- compare-rwkv-nn --color never --baseline ~/Projects/Packages/rwkv-rs-test/test_gen/rwkv_lm/bf16/case_000000 --repeat 3 --warmup 1'`.
- Remote compare result: activation stayed valid, `activation_summary compared=54 passed=54 failed=0`. Timing fully passed, `timing_summary compared=76 passed=76 failed=0 actual_total_ms=118.412 baseline_total_ms=175.887 speedup=1.49x`. `loss/l2wrap_cross_entropy` was `4.787ms` vs `5.957ms`, `1.24x`; `cells/*/channel_mixer` also passed at `1.31x`. This improves the latest repeated remote compare state (`1.45x-1.46x`, with marginal channel-mixer failures) and removes the remaining row failures.
- Decision: keep the GB10 online-softmax row algorithm unless a follow-up remote profiler shows a stale binary or hidden regression. This is a remote-specific reversal of the local negative result; do not generalize it to local hardware.
- Next command: run a short remote nsys profile for the changed binary to compare the `lm_head_l2wrap_ce_forward_row_kernel` timeline against the previous `~4.2ms` remote row kernel evidence. Use profiler output only for kernel attribution, not acceptance timing.
- Remote nsys result: nsys generated `target/rwkv-test/nsys-lm-head-online-gb10.{nsys-rep,sqlite}`. The compare status under nsys is invalid for acceptance because it used `repeat=1` against a `repeat=3` baseline and failed with timing profile mismatch. Use only the kernel stats.
- Profiler result: `lm_head_l2wrap_ce_forward_row_kernel_f__i_i32` totaled `14.185ms` across `3` launches, average `4.728ms`. The previous remote nsys evidence for the two-pass row kernel was `12.787ms` across `3` launches, average `4.262ms`. The online algorithm is therefore slower in the changed kernel on GB10 even though the standard compare total happened to pass.
- Decision update: negative attempt. Do not keep this implementation. The standard compare improvement is likely run variance or unrelated row jitter; the changed row kernel itself regressed in nsys. Revert the live code and remote file back to the two-pass max/sum row kernel, keeping this branch and note as remote negative evidence.
- Next command: revert only `lm_head_l2wrap_ce_forward_row_kernel` locally, run rustfmt and `cargo check -p rwkv-nn --features cuda`, sync the reverted kernel to remote, and run remote `cargo check -p rwkv-test --features cuda`.
- Local revert state: `lm_head_l2wrap_ce_forward_row_kernel` is back to the previous two-pass max/sum row kernel with target-logit block reduction. `rustup run nightly rustfmt crates/rwkv-nn/src/kernels/train/lm_head_l2wrap_ce/kernel.rs` passed and `cargo check -p rwkv-nn --features cuda` passed after the revert.
- Next command: sync the reverted kernel file back to `10.100.1.253` and run the remote CUDA check.
- Remote revert validation: scoped rsync of the reverted kernel completed and `cargo check -p rwkv-test --features cuda` passed on `spark-35ac`.
- Keep/revert state: the online-softmax code is not live locally or remotely. The branch and note remain as negative evidence for both local and GB10 boundaries.
- Next command: run one standard remote compare after the revert to re-establish the current remote baseline state for the next implementation branch.
- Remote compare after revert: activation stayed valid (`54/54 PASS`). Timing returned to the previous shape: `timing_summary compared=76 passed=75 failed=1 actual_total_ms=118.963 baseline_total_ms=175.887 speedup=1.48x`. The remaining failure is again `cells/cell_0000/channel_mixer.time.json` at `1.922ms` vs `1.668ms`, `0.87x`; `loss/l2wrap_cross_entropy` is better after revert (`4.372ms`, `1.36x`) than with online (`4.787ms`, `1.24x`). This confirms the online row change should stay reverted.
