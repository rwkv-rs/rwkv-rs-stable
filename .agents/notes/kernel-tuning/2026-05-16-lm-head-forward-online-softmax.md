# LM Head Forward Online Softmax

- Date: 2026-05-16.
- Branch/worktree: `kernel-tuning-lm-head-online-softmax-20260516` in the existing dirty workspace.
- Dirty-tree constraint: this checkout carries broad unrelated uncommitted workspace changes from previous train-kernel and crate-structure work. This attempt is scoped to `crates/rwkv-nn/src/kernels/train/lm_head_l2wrap_ce/kernel.rs` and, only if needed, its forward launcher.
- Prior-note search command:
  - `rg -n "lm_head_l2wrap_ce|forward row|target-logit|atomic|prune-256|online softmax|logsumexp|log-sum-exp|full-vocab|two-pass|memory-bound|DRAM" .agents/notes/kernel-tuning crates/rwkv-nn/src/kernels/train/lm_head_l2wrap_ce -S`
- Matched prior evidence:
  - `2026-05-16-lm-head-forward-target-logit.md` removed the target-logit block reduction and did not improve timing; this branch does not repeat that as the primary hypothesis.
  - `2026-05-16-lm-head-forward-atomic-loss.md` removed the finalize launch via atomic loss but remained slower and nondeterministic; this branch does not use atomics.
  - `2026-05-16-lm-head-forward-prune-256.md` filtered the slow 256 candidate but did not improve real timing; this branch leaves the existing block candidates unchanged unless the new algorithm requires a separate candidate.
  - ncu notes record the row kernel as full-vocab memory/exp dominated with high DRAM throughput, high achieved occupancy, and no spilling.
- Machine/GPU: local CUDA machine for correctness/diagnostic validation only. Remote `10.100.1.253` is the primary timing target, but current SSH fetch/run access is blocked by `Permission denied (publickey,password)`.
- Kernel/stage: CUDA BF16 `lm_head_l2wrap_ce` forward row kernel, shape `B=16,T=512,rows=8192,vocab=65536`.
- Hypothesis: replace the forward row kernel's two full-vocab passes (`max` pass, then `exp/sum` pass) with an online log-sum-exp accumulation per thread and a block reduction over `(max, sum)` pairs. This keeps one exp per element but halves full-vocab global reads for the row loss path. It may fail if the changed f32 reduction order exceeds fixture tolerances or if extra combine math offsets the bandwidth reduction.
- Candidate parameters: keep existing block-size candidates `[256,512,1024]` and `num_warps = block_size / 32`. This is an implementation algorithm change under the same candidate family, not a candidate-set expansion.
- Expected keep/revert boundary: keep only if `cargo check -p rwkv-nn --features cuda` passes, activation remains `54/54 PASS` against the trace fixture, and diagnostic timing does not regress the loss row. If activation drifts, revert the code and keep this note as numerical negative evidence. If local timing is noisy, do not claim acceptance without remote validation.
- Code change: changed `lm_head_l2wrap_ce_forward_row_kernel` from two full-vocab scans to a per-thread online log-sum-exp update. The block still reduces `row_max` first, then rescales each thread-local sum by `exp(local_max - row_max)` before the denominator reduction. The target logit is loaded directly by scalar index after the denominator; this repeats the earlier target-logit direct-load subchange only as a consequence of removing the second full-vocab pass, not as the main hypothesis.
- Formatting/compile result: `rustup run nightly rustfmt crates/rwkv-nn/src/kernels/train/lm_head_l2wrap_ce/kernel.rs` passed, and `cargo check -p rwkv-nn --features cuda` passed.
- Next command: run local trace-backed `compare-rwkv-nn --repeat 3 --warmup 1` for activation and diagnostic timing only. Local timing is not the remote acceptance source while the user is using the local GPU and remote SSH is blocked.
- Local compare result: activation passed, `activation_summary compared=54 passed=54 failed=0`; timing failed and regressed diagnostically, `timing_summary compared=76 passed=3 failed=73 ... actual_total_ms=46.461 baseline_total_ms=35.957 speedup=0.77x`. `loss/l2wrap_cross_entropy` stayed slow at `1.059ms` vs `0.717ms` (`0.68x`), and `lm_head` was `0.140ms` vs `0.034ms` (`0.24x`). Because local timing may be noisy, use this only as a negative diagnostic signal, not remote acceptance evidence.
- Next command: run focused local ncu for the forward row/finalize kernels to explain whether the online one-pass design reduced memory traffic but became compute/control-flow limited, or whether autotune selected a poor block-size candidate.
- ncu command: `ncu --target-processes all --kernel-name regex:'.*(lm_head_l2wrap_ce_forward_row|lm_head_l2wrap_ce_forward_finalize).*' --launch-count 16 --section SpeedOfLight --section Occupancy --section MemoryWorkloadAnalysis --section SchedulerStats --section WarpStateStats --csv --log-file target/rwkv-test/ncu-lm-head-online-softmax.csv target/release/rwkv-test compare-rwkv-nn --color never --repeat 1 --warmup 1`.
- ncu run caveat: the emitted compare timing is invalid because profiler instrumentation uses `repeat=1` against a `repeat=3` baseline and massively inflates `loss/l2wrap_cross_entropy` (`1349.601ms`). Use only the CSV profiler metrics.
- Next command: parse `target/rwkv-test/ncu-lm-head-online-softmax.csv` and compare selected row-kernel metrics against the earlier target-logit/atomic notes.
- ncu parse result: selected row kernel remained `block=(512,1,1)`, `grid=(8192,1,1)`. Across three row-kernel samples, average duration was about `705.6us`, DRAM throughput `88.7%`, compute throughput `41.0%`, achieved occupancy `98.6%`, eligible warps per scheduler `1.03`, average not-predicated-off threads per warp `20.78`, and local/shared spilling stayed `0`. Finalize stayed small at about `13.4us`.
- Comparison to prior notes: `2026-05-16-lm-head-forward-target-logit.md` recorded row duration `727.9us-749.3us`; `2026-05-16-lm-head-forward-atomic-loss.md` recorded atomic row duration `728-731us`. The online row kernel is slightly faster in focused ncu, but the real compare loop regressed diagnostically (`loss/l2wrap_cross_entropy` `1.059ms` vs baseline `0.717ms`, total `0.77x`). This is not enough to meet the branch keep boundary.
- Decision: negative/unfinished attempt. The one-pass online log-sum-exp design reduces the focused row-kernel duration slightly, but it does not improve the observable fused loss path on the local diagnostic run and has no remote acceptance evidence while SSH is blocked.
- Next command: revert the live `lm_head_l2wrap_ce_forward_row_kernel` implementation to the previous two-pass max/sum row kernel, then run formatting and `cargo check -p rwkv-nn --features cuda`.
- Revert state: live code was reverted to the previous two-pass max/sum row kernel with target-logit block reduction. `rustup run nightly rustfmt crates/rwkv-nn/src/kernels/train/lm_head_l2wrap_ce/kernel.rs` passed.
- Revert validation: `cargo check -p rwkv-nn --features cuda` passed after the revert.
