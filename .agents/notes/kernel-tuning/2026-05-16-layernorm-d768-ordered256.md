# LayerNorm D768 Ordered-256 Reduction

- Date: 2026-05-16.
- Branch/worktree: `kernel-tuning-layernorm-d768-ordered256-20260516` in the existing dirty workspace.
- Dirty-tree constraint: this checkout carries broad unrelated uncommitted changes and prior kernel-tuning notes. This attempt is scoped to `crates/rwkv-nn/src/kernels/train/layer_norm/{forward.rs,kernel.rs}`.
- Prior-note search command:
  - `rg -n "layer_norm|LayerNorm|BLOCK_SIZE|block256|block 256|block512|block768|ordered|split-tail|drift|value_from_first_cell|embedded_context|combined reduction|ncu|speedup" .agents/notes/kernel-tuning .agents/skills/kernel-tuning/SKILL.md /root/.codex/memories/MEMORY.md`
- Matched prior evidence:
  - Ordinary local BF16 `D=768` block `256`, `512`, and `768` candidates caused downstream activation drift; do not retry them unchanged.
  - `2026-05-16-layernorm-d768-split512.md` changed the numerical boundary and still failed activation, so this branch must use a different reduction order.
  - `2026-05-16-layernorm-safe-path-analysis.md` showed combining reductions preserved activation but regressed end-to-end timing; do not repeat combined-reduction-only changes.
  - `2026-05-16-layernorm-runtime-dispatch.md` keeps remote GB10 on ordinary `256` under a narrow hardware/shape key and keeps this local machine on the safe `1024` boundary.
  - `2026-05-16-local-current-slowrow-audit.md` shows local per-cell LayerNorm groups are still a major negative delta while activation currently passes.
- Machine/GPU: local CUDA machine, BF16 `rwkv_lm`, `B=16,T=512,D=768`, rows `8192`.
- Hypothesis: use a specialized `D=768` ordered-256 reduction candidate that launches 256 units but writes 24 contiguous warp partials in the same order the `1024` path would reduce columns `0..767`. This changes the implementation shape without using the ordinary 256 accumulation order that drifted locally.
- Candidate parameters: `block_size=256`, `num_warps=8`, deterministic `true`, only valid for BF16 `D=768` when the ordinary deterministic minimum excludes block `256`. Remote GB10 ordinary `256` remains the preferred path.
- Expected keep/revert boundary: keep only if `cargo check -p rwkv-nn --features cuda` passes, activation remains `54/54 PASS`, and standard compare improves or at least does not regress the LayerNorm rows. If activation drifts, revert the implementation and keep this note as negative numerical evidence.
- First compile result: `cargo check -p rwkv-nn --features cuda` failed. The Cube loop needed explicit integer typing for `chunk`, and the launch path borrowed `input.client` before moving `input` into the linear view. This is an implementation bug in the candidate, not performance evidence.
- Second compile result: after unrolling the three 256-column chunks and cloning the launch client, `cargo check -p rwkv-nn --features cuda` still failed because Cube inferred several integer literals as `i32` in `usize` expressions. The fix is to type those literals explicitly.
- Formatting: `rustup run nightly rustfmt crates/rwkv-nn/src/kernels/train/layer_norm/forward.rs crates/rwkv-nn/src/kernels/train/layer_norm/kernel.rs` passed.
- Compile result: `cargo check -p rwkv-nn --features cuda` passed after typing the literals.
- Compare command: `cargo run --release -p rwkv-test --features cuda -- compare-rwkv-nn --color never --repeat 3 --warmup 1`.
- Compare result: activation passed, `activation_summary compared=54 passed=54 failed=0`. Timing still failed overall but improved versus the immediately prior local audit: `timing_summary compared=76 passed=11 failed=65 ... actual_total_ms=40.874 baseline_total_ms=35.957 speedup=0.88x` versus prior `0.85x`.
- LayerNorm timing impact: `layer_norm0` improved to `0.086ms` vs baseline `0.194ms` (`2.25x`); `cells/*/pre_layer_norm_for_time_mix` improved to `1.107ms` vs `0.495ms` (`0.45x`); `cells/*/pre_layer_norm_for_channel_mix` improved to `1.185ms` vs `0.501ms` (`0.42x`). These are still slower than baseline per-cell but materially better than the prior current audit (`0.28x` and `0.31x`).
- Interim decision: keep investigating this candidate. It is not enough for the final speedup target, but it proves the ordered-256 numerical design can preserve local activation while reducing LayerNorm time. Run focused ncu before deciding whether to keep or refine.
- ncu command: `ncu --target-processes all --kernel-name regex:'.*layer_norm.*' --launch-count 30 --section SpeedOfLight --section Occupancy --section MemoryWorkloadAnalysis --section SchedulerStats --section WarpStateStats --csv --log-file target/rwkv-test/ncu-layernorm-d768-ordered256.csv target/release/rwkv-test compare-rwkv-nn --color never --repeat 1 --warmup 1`.
- ncu correctness caveat: compare timing under ncu is invalid because the run used `repeat=1` and profiler instrumentation; activation still passed.
- Autotune log result: `target/autotune/0.10.0/device-0-0-cuda/rwkv_nn-kernels-train-layer_norm-forward-layer-norm-forward.json.log` selected `block_256_d768_ordered` for the local key (`num_streaming_multiprocessors=170`, `deterministic_min_block_size=1024`). Autotune median was about `50.4us` for ordered-256 versus `93.6us` for `block_1024`.
- ncu result: the capture includes both the old `layer_norm_forward_kernel_f_` `block=(1024,1,1)` candidate and the selected `layer_norm_d768_ordered256_forward_kernel_f_` `block=(256,1,1)` candidate from tuning. Representative selected ordered-256 metrics: duration about `23.1us`, compute throughput about `81.9%`, DRAM throughput about `61.3%`, achieved occupancy about `90.7%`, eligible warps per scheduler about `1.58`, issued warp per scheduler about `0.42`, no local/shared spilling. Representative old 1024 metrics: duration about `84.2us`, compute throughput about `32.5%`, DRAM throughput about `16.6%`, achieved occupancy about `63.3%`, eligible warps per scheduler about `0.85`, issued warp per scheduler about `0.34`, no local/shared spilling.
- Decision: keep the ordered-256 candidate in the live tree for now. It is correctness-safe on the local trace and improves local LayerNorm performance, but the full local target is still unmet (`0.88x`). Next work must target remaining slow groups rather than claiming completion.
- Remote validation plan: sync only `crates/rwkv-nn/src/kernels/train/layer_norm/{forward.rs,kernel.rs}` plus this note to `caizus@10.100.1.253:~/Projects/Packages/rwkv-rs-stable`, run `cargo check -p rwkv-nn --features cuda`, then run `cargo run --release -p rwkv-test --features cuda -- compare-rwkv-nn --color never --baseline ~/Projects/Packages/rwkv-rs-test/test_gen/rwkv_lm/bf16/case_000000 --repeat 3 --warmup 1`. Expected remote behavior: ordinary `block_256` remains valid because `deterministic_min_block_size=256`, while `block_256_d768_ordered` is invalid when the ordinary 256 path is allowed.
- Remote preflight: SSH succeeded, host `spark-35ac`, GPU `NVIDIA GB10, 12.1`, remote stable code copy and regenerated baseline path both exist. The remote stable path is not a git repository, so branch/worktree discipline is enforced in the local git checkout; the remote host is a measurement copy.
- Invalid remote sync: first `rsync -av ... caizus@10.100.1.253:~/Projects/Packages/rwkv-rs-stable/` placed `forward.rs`, `kernel.rs`, and `2026-05-16-layernorm-d768-ordered256.md` in the remote repo root instead of preserving paths. Treat this as a sync mistake, not validation evidence. Remove those root files and redo path-preserving sync before compiling remotely.
- Remote sync correction: removed the three root-level mis-synced files and re-synced with `rsync -avR` so `crates/.../forward.rs`, `crates/.../kernel.rs`, and this note landed at their intended remote paths.
- Remote compile result: `ssh ... 'cd ~/Projects/Packages/rwkv-rs-stable && cargo check -p rwkv-nn --features cuda'` passed.
- Invalid remote compare: the first remote compare was interrupted from the local side while it was still compiling `rwkv-test`, leaving a remote `cargo run` process without capturable output. Killed that remote process group; do not use it as evidence. Rerun with output redirected to a remote log file.
- Remote compare command: `ssh ... 'cd ~/Projects/Packages/rwkv-rs-stable && mkdir -p target/rwkv-test && LOG=target/rwkv-test/remote-layernorm-d768-ordered256-compare.log && cargo run --release -p rwkv-test --features cuda -- compare-rwkv-nn --color never --baseline ~/Projects/Packages/rwkv-rs-test/test_gen/rwkv_lm/bf16/case_000000 --repeat 3 --warmup 1 > "$LOG" 2>&1; tail -120 "$LOG"'`.
- Remote compare result: activation passed, `activation_summary compared=54 passed=54 failed=0`; timing passed, `timing_summary compared=76 passed=76 failed=0 ... actual_total_ms=117.410 baseline_total_ms=175.887 speedup=1.50x`.
- Remote autotune result: GB10 key has `num_streaming_multiprocessors=48` and `deterministic_min_block_size=256`; `block_256` remains selected, while `block_256_d768_ordered` is skipped. This matches the intended remote policy.
- Remote decision: reject this as a remote-tuning keep. It preserves remote correctness and pass status, but it does not improve over the prior remote LayerNorm-dispatch result around `1.51x`. Because the active work is now remote-first and each accepted iteration should increase speedup, revert the ordered-256 live code while keeping this branch/note as useful local numerical evidence.
- Keep/revert state: reverted the ordered-256 candidate from `layer_norm/forward.rs` and `layer_norm/kernel.rs`; the branch and note remain as evidence. Revert validation: `rustup run nightly rustfmt crates/rwkv-nn/src/kernels/train/layer_norm/forward.rs crates/rwkv-nn/src/kernels/train/layer_norm/kernel.rs` passed, and `cargo check -p rwkv-nn --features cuda` passed locally.
- Remote revert sync: synced the reverted `layer_norm/forward.rs`, `layer_norm/kernel.rs`, and this note back to `10.100.1.253`. Remote `cargo check -p rwkv-nn --features cuda` passed after the revert sync.
- Remote revert compare command: `ssh ... 'cd ~/Projects/Packages/rwkv-rs-stable && LOG=target/rwkv-test/remote-layernorm-d768-ordered256-reverted-compare.log && cargo run --release -p rwkv-test --features cuda -- compare-rwkv-nn --color never --baseline ~/Projects/Packages/rwkv-rs-test/test_gen/rwkv_lm/bf16/case_000000 --repeat 3 --warmup 1 > "$LOG" 2>&1; tail -120 "$LOG"'`. This is a revert validation run to re-establish the remote accepted baseline after removing the rejected candidate.
- Remote revert compare result: activation passed, but timing had one edge failure: `timing_summary compared=76 passed=75 failed=1 ... actual_total_ms=117.680 baseline_total_ms=175.887 speedup=1.49x`; the failed row was `cells/cell_0000/channel_mixer` at `1.839ms` actual vs `1.668ms` baseline (`0.91x`). This confirms the remaining remote problem is a channel-mixer edge row, not LayerNorm. Keep ordered-256 reverted and move the next remote attempt to channel-mixer evidence.
