# Remote Time Mixer Evidence

- Date: 2026-05-16.
- Branch/worktree: `kernel-tuning-remote-time-mixer-evidence-20260516` in the existing dirty workspace.
- Dirty-tree constraint: this branch inherits broad unrelated workspace changes plus previous process-rule and profiler notes. This attempt is evidence gathering for the next remote implementation branch; do not edit kernel code here.
- Prior-note search command:
  - `rg -n "time_mixer|wkv7|mix6|learning_rate_gate|value_residual|key_prepare|ncu|row_tile|tile|speedup|GB10|remote" .agents/notes/kernel-tuning /root/.codex/memories/MEMORY.md -S`
- Matched prior evidence:
  - `2026-05-16-time-mixer-ncu.md`: local ncu showed `mix6` and `key_prepare` are bandwidth-heavy, while `wkv7_pretrain_forward_output_kernel` under-filled the local GPU with low achieved occupancy.
  - `2026-05-16-wkv7-row-tile16-forced.md`: forcing WKV7 `row_tile=16` passed activation but worsened timing; do not repeat that candidate restriction.
  - `2026-05-16-wkv7-full-hardware-key.md`: WKV7 autotune key was expanded with hardware fingerprint fields and kept as key-design work, but future performance work still needs launch-geometry evidence.
  - `2026-05-16-mix6-forward-hardware-key.md`: mix6 forward key-design work kept activation valid and made the directly relevant local time-mixer group faster, but was not a total acceptance result.
  - `2026-05-16-remote-channel-mixer-edge-ncu.md`: remote ncu is currently blocked by `ERR_NVGPUCTRPERM`, so this attempt cannot claim occupancy, warp, memory-throughput, register, or shared-memory metrics unless permissions change.
- Machine/GPU: remote `caizus@10.100.1.253`, `NVIDIA GB10`, compute capability `12.1`.
- Shape/dtype: CUDA BF16 `rwkv_lm`, `B=16,T=512,D=768`, rows `8192`, heads `12`, head size `64`.
- Hypothesis: the next speedup-increasing implementation should target the largest current time surface, `cells/*/time_mixer`, but only after confirming the remote GB10 selected WKV7/mix6/key_prepare tunables and per-cell timing distribution. Do not repeat forced `row_tile=16`, forced Cube, Burn reference/fusion, LocalTuner bypass, or residual-add A/B.
- Commands to run:
  - Inspect remote time-mixer `.time.json` samples for `time_mixer`, `pre_layer_norm_for_time_mix`, and related rows from the latest standard compare.
  - Pull/parse remote autotune logs for `wkv7`, `mix6`, `key_prepare`, `learning_rate_gate`, and `value_residual_gate`.
- Expected keep/revert boundary: keep evidence only. If logs are stale or missing hardware/shape keys, open a separate implementation branch for the specific key or launch-geometry fix; do not patch multiple time-mixer kernels in this evidence branch.

## Evidence

- Remote timing samples from the latest standard compare show `cells/*/time_mixer` as the largest total surface: `90.097ms` of `120.800ms` actual total. Per-cell samples are stable around `7.4-8.2ms` except `cell_0000`, which is faster at about `5.6ms` because it skips the value-residual LoRA path.
- Pre-layer-norm timing rows are much smaller and mostly around `0.09ms`, with occasional first-cell or outlier samples. This branch should not target pre-layer-norm before time-mixer internals.
- Pulled remote autotune logs under `target/rwkv-test/remote-autotune-time/`.
- Mix6 remote key: runtime `cuda`, BF16, anchored `num_elements=8388608`, `batch_size=16`, `context_len=512`, `embedded_dim=768`, `rows=8192`, hardware fields present, `max_line_size=8`, `is_in_place=false`, `deterministic=true`; selected `line_size_1` at about `407us`.
- WKV7 remote key: runtime `cuda`, BF16, `batch_size=16`, `context_len=512`, `rows=8192`, `d_model=768`, `head_size=64`, `num_heads=12`, hardware fields present, `is_in_place=false`, `deterministic=true`; selected `row_tile_64` at about `600us`.
- Missing remote tuner logs for `key_prepare`, `learning_rate_gate`, and `value_residual_gate`: no matching files were found with the current remote autotune cache glob. Either those paths are not hitting `LocalTuner` in this forward, use names not matched by the glob, or their caches were not persisted in this target directory.
- Source inspection: `TimeMixer::forward` spends time in `mix6`, three projection matmuls, multiple LoRA matmuls/activations, `key_prepare`, WKV7 pretrain, group norm, bonus elementwise, output-gate LoRA, and output projection. Current `.time.json` only times the whole module, so the next evidence gap is an internal time-mixer breakdown rather than another whole-module compare.

## Decision

- Do not repeat forced WKV7 `row_tile=16`; prior local branch already showed that higher block count lost end-to-end.
- Do not open a blind WKV7 implementation branch from the remote whole-module timing alone. On GB10, WKV7 output autotune time is only about `0.6ms`, while the module is `7.4-8.2ms` per nonzero-residual cell.
- Next concrete attempt should add a temporary/internal timing breakdown for time-mixer sub-stages on a new branch, or add profiler annotations if a non-counter profiler path is available. Keep it out of accepted public trace output unless it becomes part of the trace contract.
- Before editing trace code, try remote `nsys` as the non-counter profiler path. If available, run a short `repeat=1,warmup=1` profile filtered by CUDA API/kernel stats and use only the kernel timeline/statistics, not the profiled `.time.json`, for breakdown evidence.
- Remote profiler availability: `nsys` exists at `/usr/local/bin/nsys`, version `2025.3.2`.
- Planned nsys command: `nsys profile --trace=cuda,nvtx --stats=true --force-overwrite=true -o target/rwkv-test/nsys-remote-time-mixer target/release/rwkv-test compare-rwkv-nn --color never --baseline ~/Projects/Packages/rwkv-rs-test/test_gen/rwkv_lm/bf16/case_000000 --repeat 1 --warmup 1`.
- nsys result caveat: the profiled compare uses `repeat=1`, so all `.time.json` rows fail metadata comparison against the `repeat=3` baseline. Use only nsys kernel statistics from this run.
- nsys kernel-stat signal: the largest GPU kernel family is Burn/Cubek BF16 matmul, `matmul_entry_lhs_bf16_lhs_size_1_rhs_bf16_rhs_size_1_acc_bf16_acc_size_8`, with `122.724ms` total across `327` instances (`31.0%` of captured GPU kernel time). WKV7 output is `23.508ms` across `36` instances (`5.9%`), mix6 is `14.247ms` across `36` instances (`3.6%`), key_prepare is `9.357ms` across `36` instances (`2.4%`).
- Interpretation update: the largest remaining GB10 time surface is not the standalone WKV7 output kernel. Most time is in repeated Burn/Cubek matmul calls from projections, LoRA layers, output projection, channel mixer, and lm_head. The next evidence step is to split the dominant matmul family by shape/call context from the nsys sqlite or add NVTX/module annotations; WKV7 launch-geometry work alone cannot move total speedup enough.
- Continuation after context compaction: before any new profiler/benchmark/kernel command, use the existing `target/rwkv-test/nsys-remote-time-mixer.sqlite` only for read-only attribution. The local machine lacks `sqlite3`, so the query will use Python's standard `sqlite3` module to inspect table schema and split kernel launches by short name, launch grid/block dimensions, shared-memory/register metadata if present, and CUDA runtime correlation if exported. This does not change code and does not create acceptance timing evidence.
- Local sqlite limitation: `target/rwkv-test/nsys-remote-time-mixer.sqlite` contains only `ANALYSIS_DETAILS`, so it is an nsys stats export rather than the full CUDA timeline. Next command boundary: fetch or export the already-recorded remote `target/rwkv-test/nsys-remote-time-mixer.nsys-rep` / full sqlite from `10.100.1.253` without re-running the workload, then continue attribution from that artifact.
- Remote artifact fetch blocked: `ssh -o BatchMode=yes -o ConnectTimeout=10 caizus@10.100.1.253 ...` failed with `Permission denied (publickey,password)` for both artifact listing and `nsys --version`. Do not claim new GB10 profiler attribution until remote access is restored or the full remote `.nsys-rep` is otherwise available. Existing local full nsys sqlite files may be used only to validate the attribution method, not as remote acceptance evidence.
- Continuation after online-softmax branch close: retry the existing artifact fetch with the previously working explicit key `~/.ssh/id_ed25519_dgx_spark_windows`. This is the same evidence boundary, not a new profiler run or implementation attempt.
- Next command: switch back to branch `kernel-tuning-remote-time-mixer-evidence-20260516`, then run `ssh -i ~/.ssh/id_ed25519_dgx_spark_windows -o BatchMode=yes -o ConnectTimeout=10 caizus@10.100.1.253 'cd ~/Projects/Packages/rwkv-rs-stable && hostname && ls -lh target/rwkv-test/nsys-remote-time-mixer* 2>/dev/null || true && command -v nsys && nsys --version'`.
- Remote access retry result: explicit-key SSH succeeded on `spark-35ac`. Existing artifacts are present: `target/rwkv-test/nsys-remote-time-mixer.nsys-rep` (`430K`, May 15 18:02) and `target/rwkv-test/nsys-remote-time-mixer.sqlite` (`1.3M`, May 15 18:02). `nsys` is `/usr/local/bin/nsys`, version `2025.3.2.474-253236389321v0`.
- Next command: fetch the existing remote sqlite artifact with `rsync -e "ssh -i ~/.ssh/id_ed25519_dgx_spark_windows -o BatchMode=yes -o ConnectTimeout=10" caizus@10.100.1.253:~/Projects/Packages/rwkv-rs-stable/target/rwkv-test/nsys-remote-time-mixer.sqlite target/rwkv-test/nsys-remote-time-mixer.sqlite`.
- Fetch result: rsync completed and refreshed local `target/rwkv-test/nsys-remote-time-mixer.sqlite`.
- Next command: inspect the sqlite schema and available tables with Python's standard `sqlite3` module; use it only for read-only attribution, not acceptance timing.
- Invalid local schema command: `python` is not installed in this shell (`/bin/bash: python: command not found`).
- Next command: rerun the same read-only sqlite schema inspection with `python3`.
- Local schema result: `python3` read `target/rwkv-test/nsys-remote-time-mixer.sqlite` successfully (`1.29MB`). It is a full timeline export, not only stats: `CUPTI_ACTIVITY_KIND_KERNEL` has `2835` rows, runtime API has `12279` rows, memcpy has `891` rows, and CUDA device metadata is present.
- Next command: aggregate `CUPTI_ACTIVITY_KIND_KERNEL` by kernel `shortName`/`demangledName`, duration, count, register/thread, static/dynamic shared memory, grid, and block dimensions to identify the dominant GB10 kernel families without using blocked ncu counters.
- Kernel table columns inspected: `CUPTI_ACTIVITY_KIND_KERNEL` includes `registersPerThread`, `gridX/Y/Z`, `blockX/Y/Z`, `staticSharedMemory`, `dynamicSharedMemory`, and local/shared memory fields, so launch geometry and register/shared-memory metadata can be attributed from nsys even though occupancy and throughput counters are unavailable.
- Next command: run the actual kernel-family aggregation query and print the top rows by total GPU duration.
- First aggregation caveat: the query grouped by raw `shortName` IDs, producing numeric names (`34`, `38`, etc.). The duration/geometry numbers are usable, but names must be resolved through `StringIds` before drawing conclusions.
- Next command: rerun the aggregation joining `CUPTI_ACTIVITY_KIND_KERNEL.shortName` and `demangledName` through `StringIds`.
- Resolved kernel aggregation result: the dominant GB10 GPU kernel family is still Burn/Cubek BF16 matmul. The largest individual shape is `matmul_entry_lhs_bf16_lhs_size_1_rhs_bf16_rhs_size_1_acc_bf16_acc_size_8`, `grid=(4096,16,1)`, `block=(32,12,1)`, `regs=73`, dynamic shared memory `27648`, `3` launches totaling `48.276ms` (`16.09ms` average). Other large matmul groups include `25.512ms` across `144` launches, `21.307ms` across `36`, and `18.590ms` across `36`. Major custom kernels are WKV7 output `23.508ms/36 launches`, mix6 `14.247ms/36`, channel mixer relu-square `14.050ms/36`, `lm_head_l2wrap_ce_forward_row_kernel` `12.787ms/3`, and key_prepare `9.357ms/36`.
- Interpretation update: the largest remote surface is not a WKV7 row-tile issue and not the existing `lm_head_l2wrap_ce` row kernel alone. The `grid=(4096,16,1)` BF16 matmul shape is likely the lm_head projection over `(rows=8192, d_model=768, vocab=65536)` and costs more than the loss row kernel. The next implementation branch should inspect whether the public/model call path allows fusing lm_head projection with CE/L2Wrap, or whether that would require a broader API contract change.
- Next command: inspect the current lm_head/loss call path and fused-kernel API surface before opening a new implementation branch.
- Call-path inspection result: `RwkvLM::forward` calls `forward_logits`, which computes `self.unembed.forward(embedded_context_normalized)`, then calls `lm_head_l2wrap_ce(logits, targets)`. The existing custom primitive only accepts materialized logits plus targets. In `rwkv-test` trace writer, `model.unembed.forward(embedded_context)` is constructed before timing the `loss/l2wrap_cross_entropy` closure, so Burn's lazy execution can place the unembed matmul inside the measured loss interval. A true projection+loss fusion would need a new API boundary that accepts hidden states and unembed weights, and it would need tensor-core-grade matmul or a Burn/Cubek matmul epilogue hook; a naive row-dot custom CE kernel would likely lose tensor-core performance.
- Next command: inspect the nsys kernel timeline around the `grid=(4096,16,1)` matmul launches to verify that this shape is adjacent to `lm_head_l2wrap_ce_forward_row_kernel`, which would confirm it is the lm_head projection feeding the loss row.
- Timeline adjacency result: the three `grid=(4096,16,1)` BF16 matmul launches are immediately followed by `lm_head_l2wrap_ce_forward_row_kernel` and then `lm_head_l2wrap_ce_forward_finalize_kernel`. This confirms the largest matmul shape is the lm_head projection feeding the custom loss. Each profile iteration shows about `16.0ms` for the projection, then about `4.2ms` for the loss row kernel.
- Decision for next implementation boundary: do not hand-roll projection+loss without tensor-core matmul support. The safer next remote-only implementation attempt is to revisit the existing loss row algorithm under the changed GB10 boundary, because the row kernel is still about `4.2ms` on remote and prior online-softmax evidence was local-only.
